- [x] En `usuario_salida.html`: agregar botón “Solicitar bus (1 vez)” y área de estado.

- [x] En `funcionbuseta_usuario.js`: implementar contador de solicitudes, persistir en `localStorage`, limitar a 1 vez por usuario.

- [ ] En `funcionbuseta.js`: mostrar/consumir solicitudes agregando un campo/registro en `localStorage` para sumar en el historial.
- [ ] Ajustar `lasjuntas.html` (si aplica) para mostrar solicitudes acumuladas.
- [ ] Probar flujo completo:
  - Registrar salidas en `lasjuntas.html`.
  - Ir a `usuario_salida.html` y pedir bus.
  - Ver que se registra solo una vez y que se suma en la página de registro.
